# StockLeague - Core Dependencies
# Only direct dependencies needed for this webapp

# Flask Framework (includes Werkzeug, Jinja2, itsdangerous, click, blinker)
Flask>=3.0.0

# Flask Extensions
Flask-Session>=0.5.0
Flask-SocketIO>=5.3.0

# HTTP requests (for Finnhub API)
requests>=2.31.0

# Async server for SocketIO
eventlet>=0.33.0

# Environment variables
python-dotenv>=1.0.0

# Scientific computing (for Black-Scholes options pricing)
scipy>=1.11.0

# Sentiment analysis (for news sentiment)
vaderSentiment>=3.3.2
